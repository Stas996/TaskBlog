@model IEnumerable<TaskBlog.PresentationLayer.ViewModels.ArticleViewModel>

<h2 class="text-center">@ViewBag.Title</h2>
<hr />
    @if (Model.Count() > 0)
    {
        @ArticleHelpers.ArticlesList(Model)
    }
    else
    {
        <div class="ms-empty">
            <span class="text-center">Не опубликовано ни одной статьи</span>
            @if (User.Identity.IsAuthenticated)
            {
                <p>@Html.ActionLink("Добавить статью", "Create", "Article")</p>
            }
        </div>
    }